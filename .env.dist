NODE_ENV=development

# Dev container image build args
NODE_VERSION=22.20.0
UID=1000
GID=1000

# App
PORT=3000

# Public site URL — used for canonical links, Open Graph tags and the sitemap
VITE_SITE_URL=http://localhost:3000
